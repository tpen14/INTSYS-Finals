# backend/pytest.ini

[pytest]
testpaths = tests
addopts = -v --cov=app --cov-report=html --cov-report=term
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
